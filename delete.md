- **Delete um Arquivo*
```
cd (path_do_repositório)
ls   //listar os arquivos presentes no repositório/pasta
git rm -r (nome_do_arquivo)
git status  //para conferrir
git commit -m "mensagem sobre a remoção do arquivo"
git push origin main  //alteração feita no GitHub
```
